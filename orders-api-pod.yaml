apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    io.kompose.network/internal-proxy: "true"
    io.kompose.service: orders-api
  name: orders-api
spec:
  containers:
  - args:
    - mock
    - -d
    - -h
    - 0.0.0.0
    - /app/openapi.json
    image: maanuelmm/prism:latest
    name: orders-api
    resources: {}
    volumeMounts:
    - mountPath: /app
      name: orders-api-claim0
      readOnly: true
  restartPolicy: OnFailure
  volumes:
  - name: orders-api-claim0
    persistentVolumeClaim:
      claimName: orders-api-claim0
      readOnly: true
status: {}
